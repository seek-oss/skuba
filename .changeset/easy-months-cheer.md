---
'eslint-config-skuba': minor
---

Remove `require-extensions` rules

This removes the `require-extensions` rules which were previously used to enforce file extensions on imports in order to reduce the number of files that needed to be changed to move to ESM.
