---
'@skuba-lib/api': minor
---

Publish `@skuba-lib/api` as ESM only

`@skuba-lib/api` and its subpaths are now published as ESM only; the dual CommonJS build has been dropped. Load these entry points from an ESM module or via `import()`.
