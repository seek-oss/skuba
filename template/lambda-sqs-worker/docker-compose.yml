version: '3.7'

services:
  app:
    build:
      args:
        - NPM_READ_TOKEN
      context: .
      target: build
    environment:
      - BUILDKITE_COMMIT
      - ENVIRONMENT
      - VERSION
    init: true
    volumes:
      - /workdir/node_modules
